var a = prompt("Enter a first number.");
var b = prompt("Enter a second number to add to your first number.")

function adding(a, b) {
  return a + b;
}

println("The result is " + adding(a, b) + " .")
